# Data

`examples/` is reserved for curated input fixtures and `external/` for future
external-data configuration. The current Geant4 databases remain at the
repository root; existing readers/tests still use those locations. No database
has been moved or removed during restructuring.
